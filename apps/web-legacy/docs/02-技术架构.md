# EduChain 前端 - 技术架构 (Legacy)

## 版本信息

- **文档版本**: v1.0.0
- **更新日期**: 2026年3月9日
- **维护者**: [小铭](https://github.com/zzmingoo)

---

## 1. 整体架构

### 1.1 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户界面层                            │
│                    (React Components)                        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        路由层                                │
│                   (React Router)                             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        状态管理层                            │
│              (Context API + Local State)                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        服务层                                │
│                   (API Services)                             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        HTTP 客户端                           │
│                      (Axios)                                 │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        后端 API                              │
│                  (Spring Boot REST API)                      │
└─────────────────────────────────────────────────────────────┘
```

## 2. 目录结构

```
apps/web-legacy/
├── public/                      # 静态资源
│   ├── mockServiceWorker.js    # MSW Service Worker
│   ├── avatars/                # 头像图片
│   └── images/                 # 其他图片
│
├── src/                        # 源代码
│   ├── assets/                 # 资源文件
│   │   └── react.svg
│   │
│   ├── components/             # 组件目录
│   │   ├── admin/             # 管理员组件
│   │   ├── blockchain/        # 区块链组件
│   │   ├── common/            # 通用组件
│   │   ├── knowledge/         # 知识相关组件
│   │   ├── layout/            # 布局组件
│   │   ├── recommendation/    # 推荐组件
│   │   ├── search/            # 搜索组件
│   │   └── ui/                # UI 基础组件
│   │
│   ├── constants/             # 常量定义
│   │   ├── api.ts            # API 常量
│   │   ├── app.ts            # 应用常量
│   │   └── routes.ts         # 路由常量
│   │
│   ├── contexts/              # Context 上下文
│   │   ├── AppContext.tsx    # 应用上下文
│   │   ├── AuthContext.tsx   # 认证上下文
│   │   └── ThemeProvider.tsx # 主题上下文
│   │
│   ├── hooks/                 # 自定义 Hooks
│   │   ├── useAuthToken.ts   # 认证 Token Hook
│   │   ├── useDebounce.ts    # 防抖 Hook
│   │   ├── useDraftManager.ts # 草稿管理 Hook
│   │   ├── useErrorHandler.ts # 错误处理 Hook
│   │   ├── useLocalStorage.ts # 本地存储 Hook
│   │   ├── usePagination.ts  # 分页 Hook
│   │   ├── usePermissions.ts # 权限 Hook
│   │   ├── useTextStats.ts   # 文本统计 Hook
│   │   └── useTheme.ts       # 主题 Hook
│   │
│   ├── mock/                  # Mock 数据
│   │   ├── data/             # Mock 数据文件
│   │   ├── utils/            # Mock 工具
│   │   ├── server.ts         # MSW 服务器配置
│   │   └── README.md         # Mock 说明文档
│   │
│   ├── pages/                 # 页面组件
│   │   ├── admin/            # 管理页面
│   │   ├── auth/             # 认证页面
│   │   ├── blockchain/       # 区块链页面
│   │   ├── community/        # 社区页面
│   │   ├── error/            # 错误页面
│   │   ├── knowledge/        # 知识页面
│   │   ├── legal/            # 法律页面
│   │   ├── recommendation/   # 推荐页面
│   │   ├── resources/        # 资源页面
│   │   ├── search/           # 搜索页面
│   │   ├── user/             # 用户页面
│   │   └── Home.tsx          # 首页
│   │
│   ├── router/                # 路由配置
│   │   └── index.tsx         # 路由定义
│   │
│   ├── services/              # API 服务
│   │   ├── api.ts            # Axios 实例
│   │   ├── auth.ts           # 认证服务
│   │   ├── user.ts           # 用户服务
│   │   ├── knowledge.ts      # 知识服务
│   │   ├── blockchain.ts     # 区块链服务
│   │   ├── search.ts         # 搜索服务
│   │   ├── community.ts      # 社区服务
│   │   ├── admin.ts          # 管理服务
│   │   └── index.ts          # 服务导出
│   │
│   ├── styles/                # 样式文件
│   │   ├── globals.css       # 全局样式
│   │   ├── theme-variables.css # 主题变量
│   │   ├── theme-antd.css    # Ant Design 主题
│   │   ├── animations.css    # 动画样式
│   │   ├── glass-effects.css # 玻璃态效果
│   │   └── performance.css   # 性能优化样式
│   │
│   ├── types/                 # TypeScript 类型
│   │   ├── api.ts            # API 类型
│   │   ├── blockchain.ts     # 区块链类型
│   │   ├── common.ts         # 通用类型
│   │   └── index.ts          # 类型导出
│   │
│   ├── utils/                 # 工具函数
│   │   ├── auth.ts           # 认证工具
│   │   ├── cache.ts          # 缓存工具
│   │   ├── error.ts          # 错误处理
│   │   ├── format.ts         # 格式化工具
│   │   ├── storage.ts        # 存储工具
│   │   ├── validation.ts     # 验证工具
│   │   ├── performance.ts    # 性能工具
│   │   └── index.ts          # 工具导出
│   │
│   ├── App.tsx                # 应用根组件
│   ├── App.css                # 应用样式
│   ├── main.tsx               # 应用入口
│   └── index.css              # 入口样式
│
├── .env                       # 环境变量
├── .env.development           # 开发环境变量
├── .env.production            # 生产环境变量
├── .env.mock                  # Mock 环境变量
├── index.html                 # HTML 模板
├── package.json               # 项目配置
├── tsconfig.json              # TypeScript 配置
├── vite.config.ts             # Vite 配置
├── eslint.config.js           # ESLint 配置
└── .prettierrc                # Prettier 配置
```

## 3. 核心模块

### 3.1 路由模块

使用 React Router v7 进行路由管理：

```typescript
// src/router/index.tsx
const router = createBrowserRouter([
  {
    path: '/',
    element: <Layout />,
    children: [
      { index: true, element: <Home /> },
      { path: 'knowledge', element: <KnowledgeList /> },
      { path: 'knowledge/:id', element: <KnowledgeDetail /> },
      // ... 更多路由
    ]
  },
  {
    path: '/admin',
    element: <AdminLayout />,
    children: [
      // 管理员路由
    ]
  }
]);
```

### 3.2 状态管理

使用 Context API 进行全局状态管理：

```typescript
// AuthContext
interface AuthContextType {
  user: User | null;
  token: string | null;
  login: (credentials: LoginRequest) => Promise<void>;
  logout: () => void;
  isAuthenticated: boolean;
}

// ThemeContext
interface ThemeContextType {
  theme: 'light' | 'dark';
  toggleTheme: () => void;
}

// AppContext
interface AppContextType {
  loading: boolean;
  setLoading: (loading: boolean) => void;
  // ... 其他全局状态
}
```

### 3.3 API 服务层

统一的 API 服务封装：

```typescript
// src/services/api.ts
const apiClient = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 10000,
});

// 请求拦截器
apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 响应拦截器
apiClient.interceptors.response.use(
  (response) => response.data,
  (error) => {
    // 错误处理
    return Promise.reject(error);
  }
);
```

### 3.4 Mock 数据层

使用 MSW (Mock Service Worker) 进行数据模拟：

```typescript
// src/mock/server.ts
export const handlers = [
  rest.post('/api/auth/login', (req, res, ctx) => {
    return res(ctx.json({ token: 'mock-token', user: mockUser }));
  }),
  // ... 更多 handlers
];

export const worker = setupWorker(...handlers);
```

## 4. 数据流

### 4.1 用户认证流程

```
1. 用户输入凭证
   ↓
2. 调用 AuthContext.login()
   ↓
3. 发送 POST /api/auth/login
   ↓
4. 接收 token 和用户信息
   ↓
5. 存储到 localStorage
   ↓
6. 更新 AuthContext 状态
   ↓
7. 重定向到首页
```

### 4.2 数据请求流程

```
Component
   ↓
useState/useEffect
   ↓
Service Function
   ↓
Axios Request
   ↓
Request Interceptor (添加 token)
   ↓
Backend API / MSW Mock
   ↓
Response Interceptor (错误处理)
   ↓
Update Component State
   ↓
Re-render
```

## 5. 性能优化

### 5.1 代码分割

```typescript
// 路由懒加载
const KnowledgeDetail = lazy(() => import('./pages/knowledge/KnowledgeDetail'));
const AdminDashboard = lazy(() => import('./pages/admin/Dashboard'));

// 使用 Suspense
<Suspense fallback={<Loading />}>
  <KnowledgeDetail />
</Suspense>
```

### 5.2 组件优化

```typescript
// 使用 memo 避免不必要的重渲染
export const KnowledgeCard = memo(({ knowledge }: Props) => {
  // ...
});

// 使用 useMemo 缓存计算结果
const filteredList = useMemo(() => {
  return list.filter(item => item.status === 1);
}, [list]);

// 使用 useCallback 缓存函数
const handleClick = useCallback(() => {
  // ...
}, [dependencies]);
```

### 5.3 资源优化

```typescript
// 图片懒加载
<img loading="lazy" src={imageUrl} alt="..." />

// 组件按需导入
import { Button, Card } from 'antd';
```

## 6. 样式方案

### 6.1 CSS 模块化

- 全局样式: `src/styles/globals.css`
- 组件样式: 使用 CSS Modules 或 styled-components
- Ant Design 主题定制: `src/styles/theme-antd.css`

### 6.2 主题系统

```css
/* 亮色主题 */
:root {
  --primary-color: #1890ff;
  --bg-color: #ffffff;
  --text-color: #000000;
}

/* 暗色主题 */
[data-theme='dark'] {
  --primary-color: #177ddc;
  --bg-color: #141414;
  --text-color: #ffffff;
}
```

## 7. 类型系统

### 7.1 API 类型

```typescript
// src/types/api.ts
export interface ApiResponse<T = any> {
  code: number;
  message: string;
  data: T;
}

export interface PaginatedResponse<T> {
  content: T[];
  totalElements: number;
  totalPages: number;
  size: number;
  number: number;
}
```

### 7.2 业务类型

```typescript
// src/types/common.ts
export interface User {
  id: number;
  username: string;
  email: string;
  role: 'LEARNER' | 'ADMIN';
  // ...
}

export interface KnowledgeItem {
  id: number;
  title: string;
  content: string;
  type: ContentType;
  // ...
}
```

## 8. 错误处理

### 8.1 全局错误边界

```typescript
class ErrorBoundary extends Component {
  componentDidCatch(error: Error, errorInfo: ErrorInfo) {
    console.error('Error caught by boundary:', error, errorInfo);
  }

  render() {
    if (this.state.hasError) {
      return <ErrorPage />;
    }
    return this.props.children;
  }
}
```

### 8.2 API 错误处理

```typescript
try {
  const data = await knowledgeService.getDetail(id);
  setKnowledge(data);
} catch (error) {
  if (error.response?.status === 404) {
    navigate('/404');
  } else {
    message.error('加载失败，请重试');
  }
}
```

## 9. 构建配置

### 9.1 Vite 配置

```typescript
// vite.config.ts
export default defineConfig({
  plugins: [react()],
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'react-vendor': ['react', 'react-dom', 'react-router-dom'],
          'antd-vendor': ['antd', '@ant-design/icons'],
        }
      }
    }
  },
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
      }
    }
  }
});
```

## 10. 总结

EduChain 前端 Legacy 版本采用了经典的 React SPA 架构，具有清晰的分层设计和模块化结构。虽然已停止维护，但其架构设计和代码组织方式仍具有参考价值。

---

**下一篇**: [开发指南](./03-开发指南.md)
